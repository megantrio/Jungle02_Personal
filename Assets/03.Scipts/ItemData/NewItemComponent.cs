/*using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NewItemComponent : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        // 예시 아이템 데이터 생성
        ItemData newItem = ScriptableObject.CreateInstance<ItemData>();
        newItem.Name = "Sample Item";
        newItem.Description = "This is a sample item.";
        newItem.ItemType = ItemType.Equipable;
        newItem.AddLife = 10;
        newItem.AddAttack = 5;
        newItem.AddDefense = 3;
        newItem.AddAgility = 2;

        // 아이템 데이터 추가
        InventoryManager.Instance.availableItems.Add(newItem);
    }

}*/
